import Cocoa

// 1. Объявите три параметра. Первый из них должен быть константой типа Float с произвольным числовым значением,
// второй - пустой константой типа Float, третий - пустой переменной типа Double

let firstFloat: Float = 42.4
// в playgroud'е не компилятся пустые переменные!
let secondFloat: Float = 0.0
var thirdDouble: Double = 0.0

// 2. Установите новое произвольное значение всеем параметрам, для которых эта операция возможна
thirdDouble = 14.42
